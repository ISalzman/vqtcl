#!/usr/bin/env tclsh
# %renumber<^\s*test >%

source [file join [file dir [info script]] initests.tcl]

test 0 {} {
    package require tvq
} $version

test 1 {} {
    tvq new ""
} {bla bla}

test 2 {} {
    lua { dofile "../lvq/tests/test.lua" }
    lua { assert(dostring "?") }
    lua { print "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" }
} {bla bla}

::tcltest::cleanupTests
