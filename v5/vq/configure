if [ -f ../src/Makefile.in ]; then
    cp ../src/Makefile.in Makefile
    echo 'package ifneeded vq 5.0 \
        [list load [file join $dir libvq5[info sharedlibext]]]' >pkgIndex.tcl
else
    echo "must be run as 'sh ../configure' from inside the build directory"
fi
